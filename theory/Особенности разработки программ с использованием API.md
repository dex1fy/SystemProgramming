## Особенности разработки программ с использованием API информационной системы (основные отличия от программ, не использующих API)


**API - это как меню в ресторане** 🍽️

## Простая аналогия:

Представь, что ты в ресторане:
- **Ты (программа)** хочешь получить еду (функциональность)
- **Кухня (система/сервис)** умеет готовить
- **Меню (API)** показывает, что можно заказать и как это сделать

## Что такое API на пальцах:

**API (Application Programming Interface)** - это набор **правил и команд**, которые одна программа предоставляет другой программе для общения между собой.

## Примеры из жизни:

### 1. **Социальные сети**
```
Тебе нужно: "Показать фото из Instagram на своем сайте"
Instagram API говорит: "Используй команду GET /photos, 
я верну тебе фотографии в JSON формате"
```

### 2. **Погода**
```
Твоя программа: "Какая погода в Москве?"
Погодный API: "Отправь мне запрос с городом, 
я верну температуру, влажность и т.д."
```

### 3. **Платежи**
```
Твой сайт: "Хочу принять оплату картой"
Платежный API: "Отправь данные карты так-то, 
я скажу, прошла оплата или нет"
```

## Как выглядит API технически:

```javascript
// Пример: API для получения курса валют
// Ты отправляешь запрос:
GET https://api.bank.com/currency/USD

// API отвечает:
{
  "currency": "USD",
  "rate": 75.50,
  "last_updated": "2024-01-15"
}
```

## Типы API:

### 1. **Веб-API** - для общения через интернет
- Twitter API - чтобы твитить со своего приложения
- Google Maps API - чтобы показывать карты на сайте
- YouTube API - чтобы встроить видео

### 2. **Системные API** - для общения с операционной системой
```c
// Windows API пример:
CreateWindow(...);  // API Windows для создания окна
MessageBox(...);    // API для показа сообщения
```

### 3. **Библиотеки API** - для использования чужих функций
```python
# Python библиотека requests - это тоже API
import requests
response = requests.get('https://api.github.com')  # Используем API библиотеки
```

## Почему API так важны? 

### **Без API:**
```
Твоя программа: "Хочу погоду!"
Тебе нужно: самому собирать данные с метеостанций, 
обрабатывать, хранить историю...
```

### **С API:**
```
Твоя программа → Погодный сервис: "Дай погоду!"
Погодный сервис → Твоя программа: "Вот данные!"
```

## Реальные примеры API, которые ты используешь каждый день:

1. **Вход через Google/Facebook** на сайтах - использует их API
2. **Карты** на сайте доставки еды - Google Maps API
3. **Онлайн-оплата** - API платежных систем
4. **Поделиться в соцсетях** - API этих соцсетей
5. **Погода в телефоне** - API погодного сервиса

## Что делает API простыми словами:

**API позволяет:**
- **Не изобретать велосипед** - использовать готовые решения
- **Общаться программам** между собой
- **Расширять возможности** своей программы чужими сервисами
- **Строить сложные системы** из простых блоков

**API НЕ позволяет:**
- Залезть внутрь чужой программы и посмотреть её код
- Изменить чужую программу как хочешь
- Использовать функциональность без разрешения

**Вывод:** API - это **посредник-переводчик** между программами, который говорит: "Если хочешь что-то сделать, скажи мне вот эти специальные слова, и я всё сделаю".

--- 

Программа с API — как телефонный звонок в кол-центр, а без API — как разговор с самим собой в пустой комнате.

---

### **Чем программа с API отличается от обычной**

**Обычная программа (без API):**
- Работает сама по себе
- Все данные внутри неё или на вашем компьютере
- Скорость зависит только от вашего ПК
- Вы контролируете всё

**Программа с API:**
- Работает **как посредник** — постоянно звонит "куда-то"
- Данные хранятся **на чужом сервере** далеко в интернете
- Зависит от интернета и чужого сервиса
- Вы управляете только своей частью

---

### **Главные отличия по пунктам**

**1. Работа с ошибками — 50% кода**
Если обычная программа может сломаться только из-за своих ошибок, то программа с API ломается постоянно:
- Пропал интернет
- Удалённый сервер упал
- Сервер ответил "слишком много запросов, подожди час"
- Формат ответа изменился без предупреждения

**Что делать:** Писать много проверок: "а если нет ответа?", "а если ответ странный?", "а если надо подождать?"

**2. Ожидание ответа**
Обычная функция выполняется мгновенно. Запрос к API может идти **секунды или минуты**.

**Пример:**
```python
# Обычная программа
результат = посчитать_сумму(5, 3)  # мгновенно

# Программа с API
результат = запросить_погоду_из_интернета()  # ждём... 2... 5... 10 секунд...
```
Интерфейс не должен висеть в это время.

**3. Ключи и доступ**
Чтобы получить данные, нужно каждый раз "предъявлять пропуск" — **API-ключ или токен**. Его нужно:
- Где-то хранить (но не в открытом виде)
- Обновлять, если истёк срок
- Не показывать посторонним

**4. Лимиты**
У большинства API есть лимиты: "не больше 1000 запросов в день". Нужно считать свои запросы и не превышать.

**5. Чужой формат данных**
Данные приходят в **JSON** — как текст со структурами `{"поле": "значение"}`. Нужно этот текст разбирать и проверять, что все нужные поля есть.

---

### **Что появляется в коде с API**

1. **Функции для работы с сетью** — вместо чтения файла с диска
2. **Обработчики ошибок** на каждый чих: нет сети, сервер молчит, данные битые
3. **Таймауты** — "если нет ответа 30 секунд, прекращай ждать"
4. **Повторные попытки** — "если не получилось, попробуй ещё 2 раза через 5 секунд"
5. **Кэширование** — "сохрани ответ, чтобы не спрашивать снова"
6. **Логирование** — запись всех запросов и ответов для отладки

---

### **Простая аналогия**

**Без API:** Вы готовите ужин дома. Все продукты в холодильнике. Скорость зависит от вас.

**С API:** Вы заказываете ужин через доставку. Зависите:
- От работы ресторана (готовят ли?)
- От курьера (проедет ли?)
- От погоды (не отменит ли?)
- От приложения (не зависнет ли?)

Вы не контролируете процесс, только делаете заказ и ждёте.

---

### **Итог одним предложением**

**Программа с API — это программа, которая постоянно должна договариваться с внешним миром и готова к тому, что мир может не ответить, ответить неправильно или ответить не сразу.**